#!/bin/bash

mkdir -p .ddev/migrations
read -p "Migration name: [pull-staging] " MIGRATION_NAME
MIGRATION_NAME=${MIGRATION_NAME:-pull-staging}
wp option get wpsdb_settings --format=json --url=${DDEV_PRIMARY_URL} > ".ddev-c/migrations/${MIGRATION_NAME}.json"